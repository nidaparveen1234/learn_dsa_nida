Day 0 – Python Refresh

Today was Day 0 of my DSA journey. Since it's been some time since I last used Python, I decided to refresh the basics before starting DSA.

Practice

Q1. What's the difference between a list and a tuple?

My Answer:

List means that it can consist of things like 1, 2, 3 and they shouldn't repeat, while the tuple is different. Once it's made, it cannot be changed.

Q2. When do we use a set instead of a list?

My Answer:

Set is probably used when we have numbers that repeat.

Q3. What's the difference between `==` and `is`?

My Answer:

`==` is used for comparison. I didn't remember much about `is`.

Q4. What does this print?

```python
a = [1, 2, 3]
b = a
b.append(4)

print(a)
```

I didn't know the answer during practice.

Q5. How do you create an empty dictionary?

I forgot this one too.

Coding Practice

Q1---------------------------------

```python
for i in range(1, 21):
    print(i)
```

Q2-------------------------------

```python
for i in range(1, 21):
    if i % 2 == 1:
        continue
    print(i)
```

Q3----------------------------

My idea was to compare each element one by one until I found the largest number. I remembered the logic but forgot the Python syntax.

Q4-----------------------------------
I remembered that a dictionary was needed to count the characters, but I forgot how to write it.

Q5---------------------------------

```python
def is_even(n):
    if n % 2 == 0:
        return True
    else:
        return False
```

What I revised after the lesson

* `for` loop using `range()`
* `if`, `elif`, and `else`
* Functions with one parameter
* Empty list `[]`
* Empty tuple `()`
* Empty dictionary `{}`
* Empty set `set()`
* Printing odd numbers using `range()`

Today reminded me that I still remember the logic behind many problems, but I need to refresh my Python syntax. That's exactly why I'm starting with Day 0 before jumping into DSA.
